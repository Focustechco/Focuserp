# Worker M1 Context
Target: Update `supabase_schema.sql` with 3NF DDL tables (`tenants`, `users`, `clientes`, `cliente_contatos`, `contas_receber`, `contas_receber_parcelas`, `contas_pagar`, `contas_pagar_parcelas`, `projetos`, `audit_logs`), PostgreSQL helper function `get_auth_tenant_id()`, and multi-tenant RLS policies.
