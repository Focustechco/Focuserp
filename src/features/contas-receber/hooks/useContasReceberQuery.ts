/**
 * Hooks React Query para o módulo Financeiro — Contas a Receber
 * Re-exports do hook central para manter interface consistente por feature
 */
export { useContasReceberQuery } from '@/hooks/useSupabaseQuery';
