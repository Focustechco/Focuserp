/**
 * Hooks React Query para o módulo Financeiro — Contas a Pagar
 * Re-exports do hook central para manter interface consistente por feature
 */
export { useContasPagarQuery } from '@/hooks/useSupabaseQuery';
