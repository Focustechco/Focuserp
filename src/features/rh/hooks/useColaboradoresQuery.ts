/**
 * Hooks React Query para o módulo de RH — Colaboradores
 * Re-exports do hook central para manter interface consistente por feature
 */
export { useColaboradoresQuery } from '@/hooks/useSupabaseQuery';
